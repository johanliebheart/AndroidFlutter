import 'package:flutter/material.dart';

class AcercadeTabs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: new AppBar(
        title: new Text('Aplicación bien shila'),
      ),
      body: new Center(
        child: new Text('Qué pasó pinches perros, ya estámos aprendiendo Flutter')
      ),
    );
  }
}